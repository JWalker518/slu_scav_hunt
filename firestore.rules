rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: This is for development purposes ONLY.
      // It allows anyone with your database reference to view and edit all data.
      allow read, write: if true;
    }
  }
}
